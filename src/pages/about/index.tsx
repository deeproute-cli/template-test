import React, { memo } from 'react'

import {AboutWrapper} from './style'

export default memo(function About() {
    return (
        <AboutWrapper>
            this is about page
        </AboutWrapper>
    )
})
